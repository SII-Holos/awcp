# AWCP Experiment - Two Machines Mode (Remote Side)
# Run this on the machine that will execute tasks

MODE=remote-only

# Remote configuration
REMOTE_PORT=4001
REMOTE_URL=http://192.168.1.101:4001

# Host configuration (the other machine)
HOST_URL=http://192.168.1.100:4000

# SSH configuration (connect to Host machine)
SSH_HOST=192.168.1.100
SSH_PORT=22
SSH_USER=awcp

# Mount path on this machine
MOUNT_PATH=/tmp/awcp-experiment/mount

# Mock Agent configuration
MOCK_AGENT_TYPE=add-header

# Logging
LOG_LEVEL=info
