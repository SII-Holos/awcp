# AWCP Experiment - Local Mode Configuration
# Both Host and Remote run on the same machine

# Run mode: local | host-only | remote-only
MODE=local

# Host configuration
HOST_PORT=4000
HOST_URL=http://localhost:4000

# Remote configuration
REMOTE_PORT=4001
REMOTE_URL=http://localhost:4001

# SSH configuration (for SSHFS)
# In local mode, we connect to localhost
SSH_HOST=localhost
SSH_PORT=22
# SSH_USER will default to current user if not set

# Scenario to use
SCENARIO=basic

# Paths (relative to experiment directory)
# These will be resolved based on SCENARIO
WORKSPACE_PATH=./scenarios/${SCENARIO}/workspace
MOUNT_PATH=./scenarios/${SCENARIO}/mount

# Mock Agent configuration
MOCK_AGENT_TYPE=add-header

# Logging
LOG_LEVEL=info
